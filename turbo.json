{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "gen": {
      "outputs": ["__generated__/**"]
    },
    "build": {
      "dependsOn": ["^build", "gen"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["gen"],
      "cache": false
    }
  }
}
