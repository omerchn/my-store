{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "gen"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["gen", "validate-env"],
      "cache": false
    },
    "gen": {
      "outputs": ["__generated__/**"],
      "cache": false
    },
    "validate-env": {
      "cache": false
    }
  }
}
