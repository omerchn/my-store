{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "gen": {
      "outputs": ["generated/**"]
    },
    "build": {
      "dependsOn": ["^build", "gen"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["gen"],
      "cache": false
    }
  }
}
